apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "fly-frp-tunnel.fullname" . }}
  labels:
    {{- include "fly-frp-tunnel.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "fly-frp-tunnel.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "fly-frp-tunnel.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
